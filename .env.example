# GraphQL API endpoint for the association-hub admin portal.
# In development this can be a relative path if Vite is proxied,
# or a full URL like https://api.example.com/graphql
VITE_GRAPHQL_ENDPOINT=/graphql
